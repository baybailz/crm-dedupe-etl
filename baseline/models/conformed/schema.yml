# Conformed contracts.
version: 2

models:
  - name: dim_company
    description: The company dimension, one row per CRM company.
    columns:
      - name: company_id
        tests: [unique, not_null]
