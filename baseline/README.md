# baseline

The dbt project as it stood before this request: a staging view over the CRM
and the company dimension built from it. No purchased data, no matching.

These files are not built by `dbt build`. They live outside `model-paths`, and
exist so the presentation can show what was already there against what this
change added.
