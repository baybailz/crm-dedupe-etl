# Project config: where seeds, models, and macros live.
# Seed column types live in seeds/schema.yml.
# Materializations and tags are declared inside each model with config().
name: crm_dedupe
version: "1.0.0"
profile: crm_dedupe

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
test-paths: ["tests"]
