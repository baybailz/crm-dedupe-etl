{
 "files": [
  {
   "path": "scripts/load_purchased.py",
   "sql": "#!/usr/bin/env python3\n\"\"\"Land the next purchased company file so dbt can read it.\n\nThe ingest step. On a CDW this would be a stage: PUT the file, COPY it into a\nlanding table, models read that. Here a dbt seed plays the landing table.\n\nTakes the next file off state/loaded_files.json, stamps every row with\nsource_file and row_num (together they make record_key), and rewrites the seed\nfrom every file loaded so far. Rebuilding rather than appending is what makes a\nre-run safe: the seed is a pure function of the state file.\n\n  --action load_next   stage the next file in the queue (default)\n  --action reset       clear the queue and empty the seed\n\"\"\"\n\nimport argparse\nimport csv\nimport json\nfrom pathlib import Path\n\nROOT = Path(__file__).resolve().parents[1]\nINCOMING = ROOT / \"incoming\"\nSTATE_FILE = ROOT / \"state\" / \"loaded_files.json\"\nSEED_FILE = ROOT / \"seeds\" / \"purchased_company.csv\"\n\n# The contract stg_purchased_company reads. Change this list and the seed,\n# the record keys, and the staging model all follow from it.\nSTAMP_COLS = [\"source_file\", \"row_num\"]\nPURCHASED_COLS = [\n    \"company_name\", \"address_1\", \"address_2\", \"address_3\",\n    \"city\", \"state\", \"zip\", \"website\", \"primary_phone_number\",\n]\n\n\ndef stamped_rows(name: str) -> list[list[str]]:\n    \"\"\"One purchased file, every row tagged with its filename and line number.\"\"\"\n    with open(INCOMING / f\"{name}.csv\", newline=\"\") as fh:\n        return [[name, i] + [row.get(col, \"\") for col in PURCHASED_COLS]\n                for i, row in enumerate(csv.DictReader(fh), start=1)]\n\n\ndef write_seed(loaded: list[str]) -> int:\n    \"\"\"Rebuild the seed from the state file. Never appends.\"\"\"\n    rows = [row for name in loaded for row in stamped_rows(name)]\n    with open(SEED_FILE, \"w\", newline=\"\") as fh:\n        # Pin the line ending. Python's default is Windows-style, which would\n        # rewrite every line of the seed on each run and show up in git as a\n        # full-file change even when no data moved.\n        writer = csv.writer(fh, lineterminator=\"\\n\")\n        writer.writerow(STAMP_COLS + PURCHASED_COLS)\n        writer.writerows(rows)\n    return len(rows)\n\n\ndef main() -> None:\n    ap = argparse.ArgumentParser()\n    ap.add_argument(\"--action\", default=\"load_next\", choices=[\"load_next\", \"reset\"])\n    args = ap.parse_args()\n\n    loaded = json.loads(STATE_FILE.read_text()) if STATE_FILE.exists() else []\n\n    if args.action == \"reset\":\n        loaded = []\n        print(\"[reset] queue cleared\")\n    else:\n        queue = [f.stem for f in sorted(INCOMING.glob(\"*.csv\")) if f.stem not in loaded]\n        if queue:\n            loaded.append(queue[0])\n            print(f\"[pickup] {queue[0]}.csv\")\n        else:\n            print(\"[pickup] every file is already loaded\")\n\n    STATE_FILE.parent.mkdir(parents=True, exist_ok=True)\n    STATE_FILE.write_text(json.dumps(loaded, indent=2) + \"\\n\")\n    print(f\"[seed] {write_seed(loaded)} records from {len(loaded)} file(s)\")\n\n\nif __name__ == \"__main__\":\n    main()\n"
  },
  {
   "path": ".github/workflows/pipeline.yml",
   "sql": "# One dispatchable workflow = one pipeline run:\n# stage one company file -> dbt build (tag:master_data) -> publish JSON.\n# Fails closed: a red build commits nothing.\nname: pipeline\n\non:\n  workflow_dispatch:\n    inputs:\n      action:\n        description: \"What to do\"\n        type: choice\n        default: load_next\n        options: [load_next, reset]\n\npermissions:\n  contents: write\n\nconcurrency:\n  group: pipeline\n  cancel-in-progress: false\n\njobs:\n  run:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n          cache: pip\n\n      - name: Install dbt\n        run: pip install dbt-core dbt-duckdb duckdb\n\n      # shell: bash => pipefail, so a failing step still fails through tee\n      - name: Stage next company file (Python)\n        shell: bash\n        run: python scripts/load_purchased.py --action \"${{ inputs.action || 'load_next' }}\" 2>&1 | tee /tmp/python.log\n\n      - name: Run dbt (seeds + models + tests)\n        shell: bash\n        run: |\n          FLAGS=\"\"\n          [ \"${{ inputs.action || 'load_next' }}\" = \"reset\" ] && FLAGS=\"--full-refresh\"\n          echo \"+ dbt build --select tag:master_data $FLAGS\"\n          dbt build --select tag:master_data $FLAGS --no-use-colors 2>&1 | tee /tmp/dbt.log\n        env:\n          DBT_PROFILES_DIR: .\n\n      - name: Export tables for the web console\n        run: >\n          python scripts/export_json.py\n          --action \"${{ inputs.action || 'load_next' }}\"\n          --python-log /tmp/python.log --dbt-log /tmp/dbt.log\n\n      - name: Commit results\n        run: |\n          git config user.name \"github-actions[bot]\"\n          git config user.email \"41898282+github-actions[bot]@users.noreply.github.com\"\n          git add state seeds/purchased_company.csv docs/data\n          git diff --cached --quiet && echo \"nothing changed\" && exit 0\n          git commit -m \"pipeline: ${{ inputs.action || 'load_next' }} ($(date -u +%FT%TZ))\"\n          git push\n"
  },
  {
   "path": "dbt_project.yml",
   "sql": "# Project config: where seeds, models, and macros live.\n# Seed column types live in seeds/schema.yml.\n# Materializations and tags are declared inside each model with config().\nname: crm_dedupe\nversion: \"1.0.0\"\nprofile: crm_dedupe\n\nmodel-paths: [\"models\"]\nseed-paths: [\"seeds\"]\nmacro-paths: [\"macros\"]\ntest-paths: [\"tests\"]\n"
  },
  {
   "path": "seeds/schema.yml",
   "sql": "# Seed properties. Column types are pinned to varchar so sparse fields\n# (an all-empty address_2) don't get type-inferred as integers.\nversion: 2\n\nseeds:\n  - name: crm_company\n    description: Master company data, the system of record.\n    config:\n      tags: ['master_data']\n      column_types:\n        zip: varchar(10)\n        phone_number: varchar(20)\n\n  - name: purchased_company\n    description: Purchased company rows staged by load_purchased.py.\n    config:\n      tags: ['master_data']\n      column_types:\n        source_file: varchar\n        row_num: integer\n        company_name: varchar\n        address_1: varchar\n        address_2: varchar\n        address_3: varchar\n        city: varchar\n        state: varchar\n        zip: varchar(10)\n        website: varchar\n        primary_phone_number: varchar\n\n  - name: company_name_aliases\n    description: Steward-maintained brand aliases, natural names both sides.\n    config:\n      tags: ['master_data']\n      column_types:\n        alias: varchar\n        canonical_name: varchar\n    columns:\n      - name: alias\n        tests: [unique, not_null]\n"
  },
  {
   "path": "profiles.yml",
   "sql": "# One connection target: a local DuckDB file, zero cost, no credentials.\ncrm_dedupe:\n  target: duckdb\n  outputs:\n    duckdb:\n      type: duckdb\n      path: crm_dedupe.duckdb\n      threads: 4\n"
  },
  {
   "path": "seeds/crm_company.csv",
   "sql": "company_id,company_name,address,city,state,zip,phone_number,website,primary_contact\n1,7-Eleven,218 Peachtree Street NW,Atlanta,GA,30303,(404) 555-0100,www.7-eleven.com,Jane Smith\n2,Home Depot,2455 Paces Ferry Road SE,Atlanta,GA,30339,770-433-8211,www.homedepot.com,Mike Jones\n3,Delta Air Lines,1030 Delta Boulevard,Atlanta,GA,30354,(404) 715-2600,www.delta.com,Ana Ruiz\n4,Chick-fil-A,5200 Buffington Road,Atlanta,GA,30349,(404) 765-8000,www.chick-fil-a.com,Sam Carter\n5,Coca-Cola,1 Coca-Cola Plaza NW,Atlanta,GA,30313,(404) 676-2121,www.coca-cola.com,Priya Patel\n"
  },
  {
   "path": "seeds/purchased_company.csv",
   "sql": "source_file,row_num,company_name,address_1,address_2,address_3,city,state,zip,website,primary_phone_number\nlist_a,1,7-Eleven,218 Peachtree Street Northwest,,,Atlanta,GA,30303,7-eleven.com,404-555-0100\nlist_a,2,711,218 Peachtree St NW,,,Atlanta,GA,30303,,4045550100\nlist_a,3,\"7-Eleven, Inc.\",218 Peachtree St,Suite 100,,Atlanta,GA,30303,www.7-eleven.com,\nlist_a,4,7-Eleven,1234 Peachtree St SE,,,Atlanta,GA,30303,,404-555-0199\nlist_a,5,The Home Depot,2455 Paces Ferry Rd Southeast,,,Atlanta,GA,30339,homedepot.com,(770) 433-8211\nlist_a,6,Piedmont Coffee Co,500 10th St NW,,,Atlanta,GA,30318,piedmontcoffee.com,404-555-0142\nlist_a,7,Blue Heron Analytics,890 Ponce de Leon Ave NE,,,Atlanta,GA,30306,blueheron.io,404-555-0177\nlist_b,1,7-11,218 Peachtree NW,,,Atlanta,GA,,,404-555-0100\nlist_b,2,Home Depot #123,2455 Paces Ferry Rd SE,,,Atlanta,GA,30339,,770.433.8211\nlist_b,3,Piedmont Coffee,500 10th St NW,,,Atlanta,GA,30318,www.piedmontcoffee.com,\nlist_b,4,Delta Airlines,1030 Delta Blvd,,,Atlanta,GA,30354,delta.com,404-715-2600\nlist_b,5,Sunrise Bakery,77 Marietta St NW,,,Atlanta,GA,30303,sunrisebakeryatl.com,404-555-0161\nlist_b,6,Sunrise Bakery LLC,77 Marietta Street NW,,,Atlanta,GA,30303,,404-555-0161\nlist_b,7,Rockdale Paper Supply,1100 Sigman Road,,,Conyers,GA,30012,rockdalepaper.com,770-555-0128\nlist_c,1,Blue Heron Analytics LLC,890 Ponce de Leon Avenue NE,,,Atlanta,GA,30306,www.blueheron.io,404-555-0177\nlist_c,2,Chick-fil-A Inc,5200 Buffington Rd,,,Atlanta,GA,30349,chick-fil-a.com,404-765-8000\nlist_c,3,The Coca-Cola Company,1 Coca-Cola Plaza Northwest,,,Atlanta,GA,30313,coca-cola.com,(404) 676-2121\nlist_c,4,Peachtree Dental Group,3350 Riverwood Pkwy,Suite 1900,,Atlanta,GA,30339,peachtreedental.com,770-555-0163\nlist_c,5,Sunrise Bakery,2200 Cheshire Bridge Rd NE,,,Atlanta,GA,30324,,404-555-0186\nlist_c,6,Midtown Print Works,847 Spring Street NW,,,Atlanta,GA,30308,midtownprintworks.com,404-555-0134\nlist_d,1,Georgia Grown Logistics,4400 Fulton Industrial Blvd SW,,,Atlanta,GA,30336,gagrown.com,404-555-0158\nlist_d,2,Rockdale Paper Supply Co,1100 Sigman Rd,,,Conyers,GA,30012,www.rockdalepaper.com,770-555-0128\nlist_d,3,Delta Air Lines Inc,1030 Delta Boulevard,,,Atlanta,GA,30354,delta.com,404-715-2600\nlist_d,4,Piedmont Coffee Co LLC,500 10th Street NW,,,Atlanta,GA,30318,piedmontcoffee.com,\nlist_d,5,7-Eleven,2650 Metropolitan Pkwy SW,,,Atlanta,GA,30315,,404-555-0121\nlist_d,6,Blue Sky Staffing,1275 Peachtree St NE,Floor 6,,Atlanta,GA,30309,blueskystaffing.com,404-555-0192\n"
  },
  {
   "path": "seeds/company_name_aliases.csv",
   "sql": "alias,canonical_name\n711,7-Eleven\n7-11,7-Eleven\nThe Home Depot,Home Depot\nDelta Airlines,Delta Air Lines\n"
  },
  {
   "path": "macros/normalize.sql",
   "sql": "{#\n  Both sides of every comparison run through these, so master and purchased\n  data get cleaned the same way.\n  replace_all forces the 'g' flag: without it DuckDB replaces only the first\n  match and silently truncates the value.\n#}\n\n{% macro replace_all(expr, pattern, replacement) -%}\n  regexp_replace({{ expr }}, '{{ pattern }}', '{{ replacement }}', 'g')\n{%- endmacro %}\n\n{% macro normalize_company_name(col) -%}\n  {%- set suffixes = ' (incorporated|inc|llc|corporation|corp|company|co|ltd|lp|llp)$' -%}\n  {%- set e1 = replace_all(\"lower(coalesce(\" ~ col ~ \", ''))\", '[^a-z0-9 ]', ' ') -%}\n  {%- set e2 = replace_all(e1, ' +', ' ') -%}\n  {%- set e3 = replace_all(\"trim(\" ~ e2 ~ \")\", '^the ', '') -%}\n  {%- set e4 = replace_all(e3, suffixes, '') -%}\n  {%- set e5 = replace_all(e4, suffixes, '') -%}\n  trim({{ replace_all(e5, ' +', ' ') }})\n{%- endmacro %}\n\n{% macro normalize_address(col) -%}\n  {%- set replacements = [\n      ('northwest', 'nw'), ('northeast', 'ne'),\n      ('southwest', 'sw'), ('southeast', 'se'),\n      ('street', 'st'), ('avenue', 'ave'), ('boulevard', 'blvd'),\n      ('road', 'rd'), ('drive', 'dr'), ('lane', 'ln'),\n      ('parkway', 'pkwy'), ('highway', 'hwy'), ('suite', 'ste'),\n  ] -%}\n  {%- set e1 = replace_all(\"lower(coalesce(\" ~ col ~ \", ''))\", '[^a-z0-9 ]', ' ') -%}\n  {%- set e2 = replace_all(e1, ' +', ' ') -%}\n  {%- set padded = \"' ' || \" ~ e2 ~ \" || ' '\" -%}\n  trim({{ replace_all(usps_abbreviations(padded, replacements), ' +', ' ') }})\n{%- endmacro %}\n\n{% macro usps_abbreviations(expr, replacements) -%}\n  {%- for old, new in replacements %}replace({% endfor -%}\n  {{ expr }}\n  {%- for old, new in replacements %}, ' {{ old }} ', ' {{ new }} '){% endfor -%}\n{%- endmacro %}\n\n{% macro digits_only(col) -%}\n  nullif({{ replace_all(\"coalesce(\" ~ col ~ \", '')\", '[^0-9]', '') }}, '')\n{%- endmacro %}\n\n{% macro url_domain(col) -%}\n  nullif(\n    regexp_replace(\n      regexp_replace(\n        regexp_replace(lower(trim(coalesce({{ col }}, ''))), '^https?://', ''),\n        '^www[.]', ''),\n      '[/?#].*$', ''),\n    '')\n{%- endmacro %}\n\n{% macro street_number(col) -%}\n  nullif(regexp_extract({{ col }}, '^[0-9]+'), '')\n{%- endmacro %}\n\n{% macro similarity(a, b) -%}\n  jaro_winkler_similarity({{ a }}, {{ b }})\n{%- endmacro %}\n"
  },
  {
   "path": "models/staging/schema.yml",
   "sql": "# Staging contracts. One row per source record, match keys never blank\n# (enforced by tests/assert_match_keys_present.sql).\nversion: 2\n\nmodels:\n  - name: stg_company\n    description: Master company data with normalized match keys.\n    columns:\n      - name: company_id\n        tests: [unique, not_null]\n\n  - name: stg_purchased_company\n    description: Purchased company records with the same match keys.\n    columns:\n      - name: record_key\n        tests: [unique, not_null]\n"
  },
  {
   "path": "models/staging/stg_company.sql",
   "sql": "-- Master data with the normalized match keys the matching layer compares on.\n-- Brand aliases come from seeds/company_name_aliases.csv: stewards write\n-- natural names (711 -> 7-Eleven), the normalizing happens here.\n{{ config(materialized='view', tags=['master_data']) }}\n\nwith src as (\n    select * from {{ ref('crm_company') }}\n),\n\nnormalized as (\n    select\n        company_id,\n        company_name,\n        address,\n        city,\n        state,\n        zip,\n        phone_number,\n        website,\n        primary_contact,\n        {{ normalize_company_name('company_name') }}       as name_norm,\n        {{ normalize_address('address') }}                 as address_norm,\n        nullif(lower(trim(city)), '')                                  as city_norm,\n        nullif(upper(trim(state)), '')                                 as state_norm,\n        left({{ digits_only('zip') }}, 5)                  as zip5,\n        right({{ digits_only('phone_number') }}, 10)       as phone10,\n        {{ url_domain('website') }}                        as domain\n    from src\n)\n\nselect\n    normalized.*,\n    coalesce(nullif({{ normalize_company_name('company_name_aliases.canonical_name') }}, ''), normalized.name_norm) as name_match,\n    {{ street_number('normalized.address_norm') }}       as street_num\nfrom normalized\nleft join {{ ref('company_name_aliases') }} as company_name_aliases\n    on normalized.name_norm = {{ normalize_company_name('company_name_aliases.alias') }}\n"
  },
  {
   "path": "models/staging/stg_purchased_company.sql",
   "sql": "-- Purchased records with the same match keys as the master side, so the two\n-- compare like-for-like. record_key is source file + line number.\n{{ config(materialized='view', tags=['master_data']) }}\n\nwith src as (\n    -- The loader rewrites this seed from whatever files are loaded, then\n    -- dbt seed lands it. See scripts/load_purchased.py.\n    select * from {{ ref('purchased_company') }}\n),\n\nnormalized as (\n    select\n        source_file || '-' || lpad(cast(row_num as varchar), 3, '0') as record_key,\n        source_file,\n        row_num,\n        company_name,\n        address_1,\n        address_2,\n        address_3,\n        city,\n        state,\n        zip,\n        website,\n        primary_phone_number,\n        {{ normalize_company_name('company_name') }} as name_norm,\n        {{ normalize_address(\n            \"trim(coalesce(address_1,'') || ' ' || coalesce(address_2,'') || ' ' || coalesce(address_3,''))\"\n        ) }}                                         as address_norm,\n        nullif(lower(trim(city)), '')                            as city_norm,\n        nullif(upper(trim(state)), '')                           as state_norm,\n        left({{ digits_only('zip') }}, 5)            as zip5,\n        right({{ digits_only('primary_phone_number') }}, 10) as phone10,\n        {{ url_domain('website') }}                  as domain\n    from src\n)\n\nselect\n    normalized.*,\n    coalesce(nullif({{ normalize_company_name('company_name_aliases.canonical_name') }}, ''), normalized.name_norm) as name_match,\n    {{ street_number('normalized.address_norm') }}   as street_num\nfrom normalized\nleft join {{ ref('company_name_aliases') }} as company_name_aliases\n    on normalized.name_norm = {{ normalize_company_name('company_name_aliases.alias') }}\n"
  },
  {
   "path": "models/transform/schema.yml",
   "sql": "# Transform contracts. Grain is the candidate pair, not the record:\n# a record with no candidates has no row here.\nversion: 2\n\nmodels:\n  - name: trn_scored_pairs\n    description: Candidate pairs with fuzzy scores and classification.\n"
  },
  {
   "path": "models/transform/trn_scored_pairs.sql",
   "sql": "-- The matching engine: block, score, classify.\n--   block     candidate pairs from the equality passes listed below\n--   score     Jaro-Winkler on name + address, plus exact signals\n--   classify  duplicate or nothing; a different street number is a new location\n-- Both match sides fold into match_targets first, so the blocking and the\n-- scoring are each written once instead of once per side.\n{{ config(materialized='view', tags=['master_data']) }}\n\n-- One pass per key below. Master data is always fair game; a purchased record\n-- only compares against purchased records that came before it, so each pair is\n-- produced once and within-file order is stable.\n{% set candidate_join_keys = [\n    ['zip5'],\n    ['city_norm', 'state_norm'],\n    ['phone10'],\n    ['domain'],\n] %}\n\nwith purchased_records as (\n    select * from {{ ref('stg_purchased_company') }}\n),\n\ncrm_companies as (\n    select * from {{ ref('stg_company') }}\n),\n\nmatch_targets as (\n    -- everything a purchased record can match against, in one shape\n    select\n        cast(crm_companies.company_id as varchar)  as target_id,\n        'crm'                                    as target_kind,\n        cast(null as varchar)                    as target_file,\n        crm_companies.company_name                 as target_name,\n        crm_companies.address                      as target_address,\n        crm_companies.name_match,\n        crm_companies.address_norm,\n        crm_companies.zip5,\n        crm_companies.city_norm,\n        crm_companies.state_norm,\n        crm_companies.street_num,\n        crm_companies.phone10,\n        crm_companies.domain\n    from crm_companies\n\n    union all\n\n    select\n        purchased_records.record_key,\n        'purchased',\n        purchased_records.source_file,\n        purchased_records.company_name,\n        purchased_records.address_1,\n        purchased_records.name_match,\n        purchased_records.address_norm,\n        purchased_records.zip5,\n        purchased_records.city_norm,\n        purchased_records.state_norm,\n        purchased_records.street_num,\n        purchased_records.phone10,\n        purchased_records.domain\n    from purchased_records\n),\n\ncandidates as (\n{% for keys in candidate_join_keys %}\n    select\n        purchased_records.record_key,\n        match_targets.target_id,\n        match_targets.target_kind\n    from purchased_records\n    inner join match_targets\n        on {% for key in keys %}purchased_records.{{ key }} = match_targets.{{ key }}{{ ' and ' if not loop.last }}{% endfor %}\n\n        and (match_targets.target_kind = 'crm'\n             or match_targets.target_id < purchased_records.record_key)\n    {{ 'union' if not loop.last }}\n{% endfor %}\n),\n\npairs as (\n    select\n        purchased_records.record_key,\n        purchased_records.source_file,\n        purchased_records.company_name,\n        purchased_records.address_1,\n        purchased_records.city,\n        purchased_records.state,\n        purchased_records.zip,\n        case\n            when match_targets.target_kind = 'crm'                     then 'crm'\n            when match_targets.target_file = purchased_records.source_file   then 'within_file'\n            else 'cross_file'\n        end                                          as matched_side,\n        match_targets.target_id                       as matched_id,\n        match_targets.target_name                     as matched_name,\n        match_targets.target_address                  as matched_address,\n        {{ similarity('purchased_records.name_match', 'match_targets.name_match') }}     as name_sim,\n        {{ similarity('purchased_records.address_norm', 'match_targets.address_norm') }} as addr_sim,\n        (purchased_records.zip5 = match_targets.zip5)       as zip_match,\n        (purchased_records.city_norm = match_targets.city_norm\n            and purchased_records.state_norm = match_targets.state_norm) as city_match,\n        (purchased_records.street_num is not null\n            and purchased_records.street_num = match_targets.street_num) as street_match,\n        (purchased_records.phone10 = match_targets.phone10) as phone_match,\n        (purchased_records.domain = match_targets.domain)   as domain_match\n    from candidates\n    inner join purchased_records\n        on purchased_records.record_key = candidates.record_key\n    inner join match_targets\n        on match_targets.target_id = candidates.target_id\n        and match_targets.target_kind = candidates.target_kind\n)\n\nselect\n    *,\n    case\n        -- tier 1: name + geography + street number agree\n        when name_sim >= 0.85\n             and (coalesce(zip_match, false) or coalesce(city_match, false))\n             and coalesce(street_match, false)\n             and addr_sim >= 0.70\n            then 'duplicate'\n        -- tier 2: hard identifier (phone/domain) backs the name\n        when (coalesce(phone_match, false) or coalesce(domain_match, false))\n             and name_sim >= 0.80\n             and coalesce(street_match, false)\n            then 'duplicate'\n    end as match_class\nfrom pairs\n"
  },
  {
   "path": "models/conformed/schema.yml",
   "sql": "# Conformed contracts. One verdict per purchased record, four statuses\n# only, unique company_ids so a re-run can never double-insert.\nversion: 2\n\nmodels:\n  - name: dim_company_duplicates\n    description: Suspect pairs for the sales team, with similarity scores.\n\n  - name: dim_record_status\n    description: Disposition of every purchased record.\n    columns:\n      - name: record_key\n        tests: [unique, not_null]\n      - name: status\n        tests:\n          - not_null\n          - accepted_values:\n              arguments:\n                values:\n                  - new\n                  - duplicate_of_crm\n                  - duplicate_within_file\n                  - duplicate_cross_file\n\n  - name: dim_purchased_company\n    description: Purchased companies imported into the company dimension.\n    columns:\n      - name: company_id\n        tests: [unique, not_null]\n      - name: company_name\n        tests: [not_null]\n\n  - name: dim_company\n    description: The company dimension, master plus imported purchased rows.\n    columns:\n      - name: company_id\n        tests: [unique, not_null]\n"
  },
  {
   "path": "models/conformed/dim_company_duplicates.sql",
   "sql": "-- Deliverable 1: the duplicate report, one row per suspect pair.\n-- A row colliding with master data and three earlier purchased rows appears\n-- four times here. dim_record_status is the record-grained view.\n{{ config(materialized='table', tags=['master_data']) }}\n\nselect\n    record_key,\n    source_file,\n    company_name,\n    address_1,\n    city,\n    state,\n    zip,\n    matched_id,\n    matched_name,\n    matched_address,\n    round(name_sim, 3)      as name_similarity,\n    round(addr_sim, 3)      as address_similarity,\n    phone_match,\n    domain_match,\n    match_class\nfrom {{ ref('trn_scored_pairs') }}\nwhere match_class is not null\norder by record_key, matched_id\n"
  },
  {
   "path": "models/conformed/dim_record_status.sql",
   "sql": "-- Deliverable 2: one verdict per purchased record.\n-- Spine is staging, not the transform: a record with no candidate pairs still\n-- needs a verdict. Best match wins by priority: master, then file, else new.\n{{ config(materialized='table', tags=['master_data']) }}\n\nwith best_match as (\n    select\n        record_key,\n        matched_side,\n        matched_id,\n        matched_name,\n        match_class,\n        row_number() over (\n            partition by record_key\n            order by\n                case matched_side\n                    when 'crm' then 0\n                    when 'cross_file' then 1\n                    else 2\n                end,\n                name_sim desc,\n                matched_id\n        ) as rn\n    from {{ ref('trn_scored_pairs') }}\n    where match_class is not null\n)\n\nselect\n    stg_purchased_company.record_key,\n    stg_purchased_company.source_file,\n    stg_purchased_company.company_name,\n    stg_purchased_company.address_1,\n    stg_purchased_company.address_2,\n    stg_purchased_company.address_3,\n    stg_purchased_company.city,\n    stg_purchased_company.state,\n    stg_purchased_company.zip,\n    stg_purchased_company.website,\n    stg_purchased_company.primary_phone_number,\n    stg_purchased_company.phone10,\n    case\n        when best_match.match_class = 'duplicate' and best_match.matched_side = 'crm'\n            then 'duplicate_of_crm'\n        when best_match.match_class = 'duplicate' and best_match.matched_side = 'within_file'\n            then 'duplicate_within_file'\n        when best_match.match_class = 'duplicate' and best_match.matched_side = 'cross_file'\n            then 'duplicate_cross_file'\n        else 'new'\n    end                 as status,\n    best_match.matched_id,\n    best_match.matched_name\nfrom {{ ref('stg_purchased_company') }} as stg_purchased_company\nleft join best_match\n    on stg_purchased_company.record_key = best_match.record_key and best_match.rn = 1\norder by stg_purchased_company.record_key\n"
  },
  {
   "path": "models/conformed/dim_purchased_company.sql",
   "sql": "-- Deliverable 3: the clean insert set, mapped to the CRM schema.\n-- Only status = 'new'. New company_ids continue the CRM sequence.\n{{ config(materialized='table', tags=['master_data']) }}\n\nwith new_records as (\n    select * from {{ ref('dim_record_status') }}\n    where status = 'new'\n),\n\nmax_id as (\n    select max(company_id) as max_company_id from {{ ref('stg_company') }}\n)\n\nselect\n    max_id.max_company_id\n      + row_number() over (order by new_records.record_key)   as company_id,\n    new_records.company_name,\n    trim(coalesce(new_records.address_1, '')\n      || case when new_records.address_2 is not null and new_records.address_2 <> ''\n              then ' ' || new_records.address_2 else '' end\n      || case when new_records.address_3 is not null and new_records.address_3 <> ''\n              then ' ' || new_records.address_3 else '' end)  as address,\n    new_records.city,\n    new_records.state,\n    new_records.zip,\n    coalesce(new_records.phone10, new_records.primary_phone_number) as phone_number,\n    new_records.website,\n    cast(null as varchar)                                     as primary_contact,\n    new_records.source_file,\n    new_records.record_key                                    as source_record_key\nfrom new_records\ncross join max_id\n"
  },
  {
   "path": "models/conformed/dim_company.sql",
   "sql": "-- The company dimension: master rows plus imported purchased rows.\n-- Incremental on company_id, so a repeated run upserts instead of duplicating.\n-- A reset passes --full-refresh to rebuild from scratch.\n{{ config(\n    materialized='incremental',\n    unique_key='company_id',\n    tags=['master_data']\n) }}\n\nselect\n    company_id, company_name, address, city, state, zip,\n    phone_number, website, primary_contact,\n    'crm_company'         as source,\n    cast(null as varchar) as source_record_key\nfrom {{ ref('stg_company') }}\n\nunion all\n\nselect\n    company_id, company_name, address, city, state, zip,\n    phone_number, website, primary_contact,\n    source_file || '.csv'   as source,\n    source_record_key\nfrom {{ ref('dim_purchased_company') }}\n"
  },
  {
   "path": "tests/schema.yml",
   "sql": "# Singular test properties. Generic tests (unique, not_null,\n# accepted_values) live with their models; these stand alone.\nversion: 2\n\ndata_tests:\n  - name: assert_match_keys_present\n    description: >\n      Fails the build if any match key is blank. A blank key scores 0.0\n      against everything, so the record matches nothing and imports as\n      new: a silent duplicate in master data.\n    config:\n      tags: ['master_data']\n\n  - name: assert_unique_candidate_pairs\n    description: >\n      Fails the build if a candidate pair is produced twice. Adding a\n      blocking pass with union all instead of union would double every\n      score and inflate the duplicate report.\n    config:\n      tags: ['master_data']\n"
  },
  {
   "path": "tests/assert_match_keys_present.sql",
   "sql": "-- Guards: matching itself. A blank match key scores 0.0 against everything,\n-- so the record matches nothing and imports as new. Nothing errors.\n-- Any row returned fails the build.\n\nselect record_key as offender from {{ ref('stg_purchased_company') }}\nwhere coalesce(name_match, '') = ''\nunion all\nselect cast(company_id as varchar) from {{ ref('stg_company') }}\nwhere coalesce(name_match, '') = ''\n"
  },
  {
   "path": "tests/assert_unique_candidate_pairs.sql",
   "sql": "-- Guards: the union across the blocking passes. A pair found by two passes\n-- must be compared once, not twice, or every score double counts.\n-- Any row returned fails the build.\n\nselect\n    record_key,\n    matched_side,\n    matched_id\nfrom {{ ref('trn_scored_pairs') }}\ngroup by record_key, matched_side, matched_id\nhaving count(*) > 1\n"
  }
 ],
 "v1": [
  {
   "path": "dbt_project.yml",
   "sql": "# Project config: where seeds, models, and macros live.\nname: crm_dedupe\nversion: \"1.0.0\"\nprofile: crm_dedupe\n\nmodel-paths: [\"models\"]\nseed-paths: [\"seeds\"]\nmacro-paths: [\"macros\"]\n"
  },
  {
   "path": "profiles.yml",
   "sql": "# One connection target: a local DuckDB file, zero cost, no credentials.\ncrm_dedupe:\n  target: duckdb\n  outputs:\n    duckdb:\n      type: duckdb\n      path: crm_dedupe.duckdb\n      threads: 4\n"
  },
  {
   "path": "macros/normalize.sql",
   "sql": "{#\n  Shared SQL helpers.\n  replace_all exists because DuckDB replaces only the FIRST match unless the\n  'g' option is passed; Snowflake's regexp_replace is global already.\n#}\n\n{% macro replace_all(expr, pattern, replacement) -%}\n  {%- if target.type == 'snowflake' -%}\n    regexp_replace({{ expr }}, '{{ pattern }}', '{{ replacement }}')\n  {%- else -%}\n    regexp_replace({{ expr }}, '{{ pattern }}', '{{ replacement }}', 'g')\n  {%- endif -%}\n{%- endmacro %}\n"
  },
  {
   "path": "seeds/schema.yml",
   "sql": "# Seed properties.\nversion: 2\n\nseeds:\n  - name: crm_company\n    description: Master company data, the system of record.\n    config:\n      tags: ['master_data']\n      column_types:\n        zip: varchar(10)\n        phone_number: varchar(20)\n"
  },
  {
   "path": "seeds/crm_company.csv",
   "sql": "company_id,company_name,address,city,state,zip,phone_number,website,primary_contact\n1,7-Eleven,218 Peachtree Street NW,Atlanta,GA,30303,(404) 555-0100,www.7-eleven.com,Jane Smith\n2,Home Depot,2455 Paces Ferry Road SE,Atlanta,GA,30339,770-433-8211,www.homedepot.com,Mike Jones\n3,Delta Air Lines,1030 Delta Boulevard,Atlanta,GA,30354,(404) 715-2600,www.delta.com,Ana Ruiz\n4,Chick-fil-A,5200 Buffington Road,Atlanta,GA,30349,(404) 765-8000,www.chick-fil-a.com,Sam Carter\n5,Coca-Cola,1 Coca-Cola Plaza NW,Atlanta,GA,30313,(404) 676-2121,www.coca-cola.com,Priya Patel\n"
  },
  {
   "path": "models/staging/schema.yml",
   "sql": "# Staging contracts.\nversion: 2\n\nmodels:\n  - name: stg_company\n    description: Master company data from the CRM.\n    columns:\n      - name: company_id\n        tests: [unique, not_null]\n"
  },
  {
   "path": "models/staging/stg_company.sql",
   "sql": "-- Staging view over the CRM company table.\n-- Collapses stray whitespace so the dimension reads clean values.\n{{ config(materialized='view', tags=['master_data']) }}\n\nselect\n    company_id,\n    trim({{ replace_all(\"coalesce(company_name, '')\", ' +', ' ') }}) as company_name,\n    trim({{ replace_all(\"coalesce(address, '')\", ' +', ' ') }})      as address,\n    trim(city)          as city,\n    upper(trim(state))  as state,\n    trim(zip)           as zip,\n    phone_number,\n    website,\n    primary_contact\nfrom {{ ref('crm_company') }}\n"
  },
  {
   "path": "models/conformed/schema.yml",
   "sql": "# Conformed contracts.\nversion: 2\n\nmodels:\n  - name: dim_company\n    description: The company dimension, one row per CRM company.\n    columns:\n      - name: company_id\n        tests: [unique, not_null]\n"
  },
  {
   "path": "models/conformed/dim_company.sql",
   "sql": "-- The company dimension. One row per company in the CRM.\n{{ config(materialized='table', tags=['master_data']) }}\n\nselect\n    company_id,\n    company_name,\n    address,\n    city,\n    state,\n    zip,\n    phone_number,\n    website,\n    primary_contact\nfrom {{ ref('stg_company') }}\n"
  }
 ],
 "compiled": {
  "models/transform/trn_scored_pairs.sql": "-- The matching engine: block, score, classify.\n--   block     candidate pairs from the equality passes listed below\n--   score     Jaro-Winkler on name + address, plus exact signals\n--   classify  duplicate or nothing; a different street number is a new location\n-- Both match sides fold into match_targets first, so the blocking and the\n-- scoring are each written once instead of once per side.\n\n\n-- One pass per key below. Master data is always fair game; a purchased record\n-- only compares against purchased records that came before it, so each pair is\n-- produced once and within-file order is stable.\n\n\nwith purchased_records as (\n    select * from \"crm_dedupe\".\"main\".\"stg_purchased_company\"\n),\n\ncrm_companies as (\n    select * from \"crm_dedupe\".\"main\".\"stg_company\"\n),\n\nmatch_targets as (\n    -- everything a purchased record can match against, in one shape\n    select\n        cast(crm_companies.company_id as varchar)  as target_id,\n        'crm'                                    as target_kind,\n        cast(null as varchar)                    as target_file,\n        crm_companies.company_name                 as target_name,\n        crm_companies.address                      as target_address,\n        crm_companies.name_match,\n        crm_companies.address_norm,\n        crm_companies.zip5,\n        crm_companies.city_norm,\n        crm_companies.state_norm,\n        crm_companies.street_num,\n        crm_companies.phone10,\n        crm_companies.domain\n    from crm_companies\n\n    union all\n\n    select\n        purchased_records.record_key,\n        'purchased',\n        purchased_records.source_file,\n        purchased_records.company_name,\n        purchased_records.address_1,\n        purchased_records.name_match,\n        purchased_records.address_norm,\n        purchased_records.zip5,\n        purchased_records.city_norm,\n        purchased_records.state_norm,\n        purchased_records.street_num,\n        purchased_records.phone10,\n        purchased_records.domain\n    from purchased_records\n),\n\ncandidates as (\n\n    select\n        purchased_records.record_key,\n        match_targets.target_id,\n        match_targets.target_kind\n    from purchased_records\n    inner join match_targets\n        on purchased_records.zip5 = match_targets.zip5\n\n        and (match_targets.target_kind = 'crm'\n             or match_targets.target_id < purchased_records.record_key)\n    union\n\n    select\n        purchased_records.record_key,\n        match_targets.target_id,\n        match_targets.target_kind\n    from purchased_records\n    inner join match_targets\n        on purchased_records.city_norm = match_targets.city_norm and purchased_records.state_norm = match_targets.state_norm\n\n        and (match_targets.target_kind = 'crm'\n             or match_targets.target_id < purchased_records.record_key)\n    union\n\n    select\n        purchased_records.record_key,\n        match_targets.target_id,\n        match_targets.target_kind\n    from purchased_records\n    inner join match_targets\n        on purchased_records.phone10 = match_targets.phone10\n\n        and (match_targets.target_kind = 'crm'\n             or match_targets.target_id < purchased_records.record_key)\n    union\n\n    select\n        purchased_records.record_key,\n        match_targets.target_id,\n        match_targets.target_kind\n    from purchased_records\n    inner join match_targets\n        on purchased_records.domain = match_targets.domain\n\n        and (match_targets.target_kind = 'crm'\n             or match_targets.target_id < purchased_records.record_key)\n    \n\n),\n\npairs as (\n    select\n        purchased_records.record_key,\n        purchased_records.source_file,\n        purchased_records.company_name,\n        purchased_records.address_1,\n        purchased_records.city,\n        purchased_records.state,\n        purchased_records.zip,\n        case\n            when match_targets.target_kind = 'crm'                     then 'crm'\n            when match_targets.target_file = purchased_records.source_file   then 'within_file'\n            else 'cross_file'\n        end                                          as matched_side,\n        match_targets.target_id                       as matched_id,\n        match_targets.target_name                     as matched_name,\n        match_targets.target_address                  as matched_address,\n        jaro_winkler_similarity(purchased_records.name_match, match_targets.name_match)     as name_sim,\n        jaro_winkler_similarity(purchased_records.address_norm, match_targets.address_norm) as addr_sim,\n        (purchased_records.zip5 = match_targets.zip5)       as zip_match,\n        (purchased_records.city_norm = match_targets.city_norm\n            and purchased_records.state_norm = match_targets.state_norm) as city_match,\n        (purchased_records.street_num is not null\n            and purchased_records.street_num = match_targets.street_num) as street_match,\n        (purchased_records.phone10 = match_targets.phone10) as phone_match,\n        (purchased_records.domain = match_targets.domain)   as domain_match\n    from candidates\n    inner join purchased_records\n        on purchased_records.record_key = candidates.record_key\n    inner join match_targets\n        on match_targets.target_id = candidates.target_id\n        and match_targets.target_kind = candidates.target_kind\n)\n\nselect\n    *,\n    case\n        -- tier 1: name + geography + street number agree\n        when name_sim >= 0.85\n             and (coalesce(zip_match, false) or coalesce(city_match, false))\n             and coalesce(street_match, false)\n             and addr_sim >= 0.70\n            then 'duplicate'\n        -- tier 2: hard identifier (phone/domain) backs the name\n        when (coalesce(phone_match, false) or coalesce(domain_match, false))\n             and name_sim >= 0.80\n             and coalesce(street_match, false)\n            then 'duplicate'\n    end as match_class\nfrom pairs"
 }
}
